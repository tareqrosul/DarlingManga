#1. Install Python and while doing so make sure to check the "Add Python 3.x.x to PATH"
#2. Open cmd/powershell in the current directory and run the following command "pip install -r requirements.txt"
#3. then run the follwoing command "python darling.py"